name: atera-mcp-server
metadata:
  template: atera-mcp-server
services:
  api:
    project: .
    language: js
    host: function
    dist: .azure-deploy
    resourceName: ${AZURE_FUNCTION_NAME}
    hooks:
      prepackage:
        run: npm run build && node scripts/prepare-deploy.js
